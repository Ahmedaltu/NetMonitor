# Prometheus configuration for NetMonitor
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "netmonitor"
    static_configs:
      - targets: ["netmonitor:8000"]
    metrics_path: /metrics
    scrape_interval: 10s
